
import { createStore } from 'framework7';

const store = createStore({
  state: {
    personSearchResults: [],
    state: '',
    district: '',
  },
  getters: {
  },
  actions: {
  }
})
export default store;
